<h1> Artist name: <%= @artist.name %> </h1>
<h2> Artist ID: <%= @artist.id %> </h2>

<% @artist.genres.each do |genre| %>
  <li><a href="/genres/<%= genre.slug %>"> <%= genre.name %></a></li>
<% end %>

<% @artist.songs.each do |song| %>
  <li><a href="/songs/<%= song.slug %>"> <%= song.name %></a></p>
<% end %>
